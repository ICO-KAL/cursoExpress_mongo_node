const caja = [{id:1, nombre: 'Isaac Concepcion Peralta'},{id:2, nombre: 'Isaac Concepcion Peralta'}];

for(let [key, value] of Object.entries(caja)){
    console.log(`los objectos de caja son ${key},${value}`);
}